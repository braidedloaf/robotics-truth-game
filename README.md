make sure all requirements are installed by running the following in the same folder of `start.py, block_sim.py`:
`pip install -r requirements.txt`

run with:
`python start.py`

change values `SCREEN_W` and `SCREEN_H` to adjust the size of display if incorrect.

change `CAM_IDX` by 1 depending on what camera you want.